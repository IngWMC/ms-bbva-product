spring:
  application:
    name: microservicio-product

  # Credenciales para acceder al config-server
  cloud:
    config:
      uri: http://localhost:7075
      username: root
      password: s3cr3t0
